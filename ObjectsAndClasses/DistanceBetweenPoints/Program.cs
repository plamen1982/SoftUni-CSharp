using System;
using System.Linq;

namespace DistanceBetweenPoints
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] firstCoordInput = Console.ReadLine()
                                    .Split()
                                    .Select(int.Parse)
                                    .ToArray();
            int[] firstCoordInput = Console.ReadLine()
                        .Split()
                        .Select(int.Parse)
                        .ToArray();
        }
    }
}
